[project]
name = "roundtable-arc-agent"
version = "0.1.0"
description = "TheRoundTable Council Agent for ARC-AGI-3 — multi-agent deliberation meets interactive reasoning"
requires-python = ">=3.12"
dependencies = [
    "arc-agi>=0.9.6",
    "requests>=2.32.0",
]

[project.scripts]
council-agent = "council_agent:main"
